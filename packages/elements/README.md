# @nlqdb/elements

Web components: `<nlq-data>` (read; Phase 1), `<nlq-action>` (write;
Phase 2), eventually `<nlq-stream>` (Phase 4+). Distributed via
`elements.nlqdb.com` → R2 as a single JS module.

See DESIGN §4.6 and IMPLEMENTATION §4 for the `<nlq-data>` v0 scope.

Not yet implemented.
