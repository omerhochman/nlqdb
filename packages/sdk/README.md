# @nlqdb/sdk

TypeScript client SDK wrapping the `/v1/*` HTTP API. Phase 0 (first
consumer is `apps/web`; second is `packages/elements`).

Published to npm as `@nlqdb/sdk` (DESIGN §5.1). Tiny, zero-dep, works
in browsers and Workers alike.

Not yet implemented.
